/** Default fit % for daily batches. Lower = more leads pass; raise once quality is good. */
export const DEFAULT_FIT_THRESHOLD = 45;

/** Used when the ICP form does not override weights. */
export const DEFAULT_COMPANY_SIZE_MIN: number | null = null;
export const DEFAULT_COMPANY_SIZE_MAX: number | null = null;
